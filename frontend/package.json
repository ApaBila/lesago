{
  "name": "frontend-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "devDependencies": {
    "vite": "^7.1.6"
  },
  "dependencies": {
    "chart.js": "^4.5.0",
    "three": "^0.180.0",
    "wordcloud": "^1.2.3"
  }
}
